# 2026-05-17 1차 회의록

## 회의 정보

- 날짜: 2026-05-17
- 시간: 1.5h
- 참석자: `JAE`, `TAE`, `JONG`
- 회의 목적: 터미널 기반 뱀파이어 서바이벌 C MVP의 범위, 역할, 협업 방식을 정리한다.

## 역할 정리

- `TAE`: 이동, 몬스터
- `JONG`: 무기, UI
- `JAE`: 게임 구조 최적화, GitHub 저장소 준비, MVP 통합

## 회의에서 나온 핵심 내용

- 난이도는 `easy`, `hard`를 고려한다.
- 적은 3종으로 시작한다.
  - HP 1
  - HP 3
  - HP 40
- 적 디자인 후보:
  - 박쥐
  - 좀비
  - 뱀파이어
- 적 공격 방식은 우선 모두 근접형으로 둔다.
- 경험치 아이템은 별 1개로 표현한다.
- 무기 후보는 4개를 고려한다.
  - 동그라미
  - 세모
  - 네모
  - 별
- 5월 30일까지 설명 가능한 MVP를 완성하는 것을 목표로 한다.
- GitHub 저장소를 만들고 각자 브랜치에서 작업한 뒤 push한다.
- 매주 각자의 계획을 공유하고, 좋은 기능이 있으면 브랜치에 추가한 뒤 제안한다.

## 결정 사항

- MVP는 먼저 터미널 환경에서 구현한다.
- 이름은 문서와 브랜치에서 `JAE`, `TAE`, `JONG`으로 통일한다.
- 브랜치는 `JAE`, `TAE`, `JONG`으로 나누고, 안정 버전은 `main`에 둔다.
- 직접 `main`에 push하지 않고 Pull Request로 합친다.
- 커밋 메시지는 한글로 작성한다.

## 다음 액션

- `JAE`: 현재 MVP를 정리하고 문서화한다.
- `TAE`: 이동과 몬스터 관련 개선 계획을 정리한다.
- `JONG`: 무기와 UI 관련 개선 계획을 정리한다.
- 전원: 다음 작업 전 `git pull`로 최신 내용을 받은 뒤 각자 브랜치에서 작업한다.
